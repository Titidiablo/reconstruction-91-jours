name: Deploy V61 dashboard now (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Le workflow V61 est obsolète. Utiliser Deploy V63."
